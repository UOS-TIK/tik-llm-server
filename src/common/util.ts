import { entries, filter, fromEntries, pipe, reduce } from '@fxts/core';
import { assert } from 'typia';

export const util = {
  // functional
  entries,
  filter,
  fromEntries,
  pipe,
  reduce,
  // validation
  assert,
};
